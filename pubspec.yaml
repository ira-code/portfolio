name: portfolio
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  provider: ^5.0.0
  firebase_core: ^1.1.0
  cloud_firestore: ^1.0.7
  firebase_analytics: ^8.0.2
  firebase_auth: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - images/kill-la-kill-game_291485638_1563439235773.png